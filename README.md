# Wifi command emulator for Mac OSX

First, 

```
cp config.sample config
vim config
```

Then, fill in the SSID and PASS of your target wifi network.

After editing the config file, execute the following command:

```
./install
```

Finally:
```
wifi
```
